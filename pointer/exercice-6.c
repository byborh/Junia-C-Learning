#include <stdio.h>

int main() {

    /*
        Étant donné un tableau de n entiers, écrire un programme principal faisant appel à des fonctions
        permettant de :
        1. remplir le tableau. La saisie du nombre d'éléments doit se faire à l'intérieur de la fonction.
        2. afficher le tableau.
        3. ajouter un élément au tableau.
        4. chercher un élément dans le tableau. La position doit être renvoyée en paramètres si
        l'élément est trouvé, sinon la valeur renvoyée est -1.
        5. modifier la première occurrence d'un élément donné.
        4
        6. modifier toutes les occurrences d'un élément donné.
        7. supprimer la première occurrence d'un élément donné.
        8. supprimer toutes les occurrences d'un élément donné.
        NB : Toutes les fonctions doivent avoir un type de retour void.
    */

    return 0;
}